[project]
name = "tms-api-tests"
version = "0.1.0"
description = "Contract tests for TMS API using Schemathesis"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "schemathesis>=4.0",
    "pytest>=8.0",
]

[tool.uv]
dev-dependencies = [
    "schemathesis>=4.0",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
